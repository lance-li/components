import { TouchSelectDirective } from './touch-select.directive';

describe('TouchSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchSelectDirective();
    expect(directive).toBeTruthy();
  });
});
